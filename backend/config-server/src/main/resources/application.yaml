server:
  port: 8888

spring:
  application:
    name: ConfigServer
  config:
    import: optional:file:.env[.properties]
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/